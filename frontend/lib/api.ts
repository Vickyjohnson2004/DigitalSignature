import axios from 'axios';export const api=axios.create({baseURL:process.env.NEXT_PUBLIC_API_URL||'http://localhost:4000/api'});api.interceptors.request.use(c=>{if(typeof window!=='undefined'){const t=localStorage.getItem('dss_token');if(t)c.headers.Authorization=`Bearer ${t}`;}return c});export function saveSession(data:any){localStorage.setItem('dss_token',data.token);localStorage.setItem('dss_user',JSON.stringify(data.user))}export function logout(){localStorage.removeItem('dss_token');localStorage.removeItem('dss_user');window.location.href='/login'}
