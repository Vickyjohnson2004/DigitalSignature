import { Schema, model, Types } from 'mongoose';
const schema=new Schema({userId:{type:Types.ObjectId,ref:'User',required:true,index:true},documentId:{type:Types.ObjectId,ref:'Document',required:true},algorithm:{type:String,required:true,index:true},runs:{type:Number,required:true},signingTime:{type:Number,required:true},verificationTime:{type:Number,required:true},signatureSize:{type:Number,required:true},keySize:{type:Number,required:true},memoryUsage:{type:Number},benchmarkDate:{type:Date,default:Date.now}}); schema.index({userId:1,benchmarkDate:-1}); export const Benchmark=model('Benchmark',schema);
